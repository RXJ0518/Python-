变量名、函数名等都是标识符

标识符的命名规则如下：
  (1)标识符以数字，字母，下划线组成，且不能以数字开头
  (2)标识符是区分大小写的 例如 ：name 和 NAME 是不同的标识符
  (3)标识符不可以使用关键字 例如：if(关键字)不能作为标识符
  
类名：使用大写字母开头的单词(CapWorld) 
模块名：应该用小写字母加下划线的方式(low_woth_)

具有特殊功能的标识符称为关键字
关键字是python语言自己已经使用的，不允许开发者自己定义和关键字相同的标识符。
常用关键字如下(还有很多就不一一举例)：
  if false def while for pass 
想查询关键字的信息，可以输入help()命令进入帮助系统查看，代码如下：
  >>>help()        #进入帮助系统
  help>keywords    #查看所有的关键字
  help>return      #查看return这个关键字的说明
  help>quit        #查看帮助系统
